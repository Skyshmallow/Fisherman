<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fisherman">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fisherman">
        <activity
            android:name=".Logo_Activity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Fisherman.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Text_Content_Activity" android:theme="@style/Theme.Fisherman"/>
        <activity android:name=".MainActivity" android:theme="@style/Theme.Fisherman.NoActionBar" />
        <activity android:name=".settings.SettingsActivity" android:theme="@style/Theme.Fisherman"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>